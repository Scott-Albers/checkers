/* eslint-disable jsx-quotes, react/prop-types, max-len, no-underscore-dangle */

import React from 'react';

export default function () {
  // const cls = props.square.isDark ? 'dark' : 'light';
  return <div data-id={1} className={'dark'}>{'x'}</div>;
  // return <div data-id={props.square._id} >{props.square.name}</div>;
}
